plugins:
  title: Plugins
  subtitle: Device plugins add hardware support without rebuilding the daemon.
  counts: "%{count} scripts · %{active} active"
  empty_title: No plugins installed
  empty_hint: Add a Lua plugin script to extend HALO with new devices.
  add: Add plugin
  # Pending changes banner
  pending_changes: You have unapplied plugin changes. Apply them to hand devices to or from their plugin.
  apply_changes: Apply changes
  # Detail panel
  type_device: Driver
  type_effect: Effects
  type_integration: Integration
  author: AUTHOR
  capabilities: CAPABILITIES
  targets: TARGET DEVICES
  effects: EFFECTS
  status_active: Active
  status_disabled: Disabled
  status_needs_permission: Needs permission
  enable: Enable
  disable: Disable
  delete: Delete script
  builtin_note: Built-in — cannot be deleted
  # Permissions
  permissions: PERMISSIONS
  permissions_pending: This plugin needs permissions you haven't granted yet — it stays inactive until you decide.
  permissions_not_granted_tag: Not granted
  permissions_enable_hint: Enabling this plugin will request the permissions above.
  permissions_granted: Granted
  grant: Grant permissions
  revoke: Revoke & disable
  permissions_builtin_note: Built-in — permissions are always granted and can't be revoked.
  permission_network: Network
  permission_os: OS
  permission_secure_storage: Secure storage
  permission_network_desc: Opens network connections (e.g. to a local SDK/server). A malicious script could send data off this machine.
  permission_os_desc: Reads the system clock (os.time / os.clock). Low risk - no file or process access is ever granted.
  permission_secure_storage_desc: Reads this plugin's own saved secret values (e.g. an API token) in clear text while it runs.
  # Config fields
  settings: SETTINGS
  save_settings: Save
  secret_set_hint: (a secret is stored)
  # Permission consent dialog (shown when enabling a plugin that declares permissions)
  consent_title: Grant permissions
  consent_body: "%{name} needs the following to run."
  consent_warning: Permissions are granted as a package. You can revoke them anytime — which disables the plugin.
  consent_modified: This plugin's script changed since you last allowed it — review the permissions again.
  consent_grant_enable: Grant & enable
  consent_cancel: Cancel
  consent_deny: Not now
  # Add dialog
  add_title: Add a plugin
  add_sub: Install a Lua device driver from a file or paste its source.
  tab_upload: Upload .lua
  tab_paste: Paste code
  choose_file: Choose .lua file…
  upload_hint: Choose a .lua file
  upload_sub: It is copied into your plugins folder.
  paste_hint: "-- paste your Lua driver here"
  name: Name
  name_hint: e.g. Nanoleaf Shapes
  cancel: Cancel
  # Delete confirmation
  delete_title: Delete script?
  delete_body: This permanently removes %{name} from your plugins folder.
