plugins:
  title: Plugins
  subtitle: Device plugins add hardware support without rebuilding the daemon.
  counts: "%{count} scripts · %{active} active"
  empty_title: No plugins installed
  empty_hint: Add a Lua plugin script to extend HALO with new devices.
  add: Add plugin
  # Plugin repositories
  repos_title: Repositories
  repos_check_updates: Check for updates
  repos_checking: Checking for updates…
  repos_url_hint: Git URL (https://...)
  repos_branch_hint: Branch (optional)
  repos_branch_label: BRANCH
  repos_branch_default: Default branch
  repos_branch_loading: Loading branches…
  repos_branch_enter_url: Enter a URL first
  repos_add: Add
  repos_add_title: Add repository
  repos_add_sub: Point HALO at a Git remote to browse, install, and update its plugins.
  repos_empty: No plugin repositories registered.
  repos_update: Update
  repos_remove: Remove repository
  repos_remove_title: Remove repository?
  repos_remove_body: This unregisters %{name} and uninstalls every plugin it provides.
  repo_source: SOURCE
  repo_last_sync: LAST SYNC
  repo_never_synced: never
  repo_drivers: DRIVERS
  repo_drivers_count: "%{count} installed"
  repo_drivers_from: DRIVERS FROM THIS REPOSITORY
  repo_no_drivers: No drivers installed from this repository yet.
  # Per-plugin updates
  updates_available: "%{count} plugin update available"
  update_all: Update all
  updating: Updating…
  update_available: Update available
  modified_on_disk: Disabled — modified on disk
  modified_on_disk_sub: This plugin's files changed since they were installed, so it was disabled for safety. Re-enable it to accept the change.
  # Pending changes banner
  pending_changes: You have unapplied plugin changes. Apply them to hand devices to or from their plugin.
  apply_changes: Apply changes
  # Detail panel
  type_device: Driver
  type_effect: Effects
  type_integration: Integration
  author: AUTHOR
  capabilities: CAPABILITIES
  targets: TARGET DEVICES
  effects: EFFECTS
  status_active: Active
  status_disabled: Disabled
  status_needs_permission: Needs permission
  enable: Enable
  disable: Disable
  delete: Delete script
  repo_sourced_note: Provided by a repository — remove the repository to uninstall
  # Permissions
  permissions: PERMISSIONS
  permissions_pending: This plugin needs permissions you haven't granted yet — it stays inactive until you decide.
  permissions_not_granted_tag: Not granted
  permissions_enable_hint: Enabling this plugin will request the permissions above.
  permissions_granted: Granted
  grant: Grant permissions
  revoke: Revoke & disable
  permission_network: Network
  permission_os: OS
  permission_secure_storage: Secure storage
  permission_network_desc: Opens network connections (e.g. to a local SDK/server). A malicious script could send data off this machine.
  permission_os_desc: Reads the system clock (os.time / os.clock). Low risk - no file or process access is ever granted.
  permission_secure_storage_desc: Reads this plugin's own saved secret values (e.g. an API token) in clear text while it runs.
  # Config fields
  settings: SETTINGS
  save_settings: Save
  secret_set_hint: (a secret is stored)
  # Permission consent dialog (shown when enabling a plugin that declares permissions)
  consent_title: Grant permissions
  consent_body: "%{name} needs the following to run."
  consent_warning: Permissions are granted as a package. You can revoke them anytime — which disables the plugin.
  consent_modified: This plugin's script changed since you last allowed it — review the permissions again.
  consent_permission_added: An update to this plugin added a new permission that must be granted before it can run again. Review what it now requires below.
  consent_grant_enable: Grant & enable
  consent_cancel: Cancel
  consent_deny: Not now
  # First-run consent to contact GitHub for official plugins and updates
  download_consent_title: Download official plugins?
  download_consent_body: HaloDaemon can download official plugins from GitHub and check for updates on startup. Nothing is downloaded until you allow it.
  download_consent_note: You can change this anytime in Settings. Plugins you add yourself are unaffected.
  download_consent_allow: Allow
  download_consent_deny: Don't allow
  # Add dialog
  add_title: Add a plugin
  add_sub: Install a plugin package — a folder containing plugin.yaml and its entry script.
  choose_folder: Choose folder…
  upload_hint: Choose a plugin package folder
  upload_sub: It is copied into your plugins folder.
  cancel: Cancel
  # Delete confirmation
  delete_title: Delete script?
  delete_body: This permanently removes %{name} from your plugins folder.
